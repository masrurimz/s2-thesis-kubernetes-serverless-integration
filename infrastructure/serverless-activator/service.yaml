apiVersion: v1
kind: Service
metadata:
  name: serverless-activator
  labels:
    app: serverless-activator
spec:
  type: ClusterIP
  selector:
    app: serverless-activator
  ports:
    - name: http
      port: 80
      targetPort: 8081
      protocol: TCP
